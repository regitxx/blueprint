# Blueprint — Team Worklog · Fri Jul 17, late session

Live app: **https://blueprint-hackathon.ai.studio** · Repo: **github.com/regitxx/blueprint** · Event: Sun Jul 19, hacking 11:30–14:30, submissions 14:30 sharp (see RUNBOOK.md for the full day plan).

## What Blueprint is (30 seconds)
Type a product idea → three agents run a real research sprint: **Scout** pulls live arXiv papers + GitHub repos, **Analyst** extracts what works (Gemini structured output), **Architect** drafts 2–3 compared variants (Fast MVP / Scalable / Research-grade) — diagrams, trade-off table, and every component cites its source. Honesty is structural: no source, no block. No mock data anywhere — the example blueprints in the app are saved live runs.

## How the system is organized (read before touching anything)
- **GitHub repo = single source of truth.** All code changes go through PRs to the repo. Never edit code inside AI Studio.
- **Google AI Studio = deploy shell only.** We paste `AISTUDIO_PASTE.md` into a Build-mode app and hit Publish → Cloud Run. It runs on [OWNER]'s Google account + billed project; the deployed app uses [OWNER]'s API key **server-side**.
- Headless proofs exist for everything: `npm run typecheck` · `npm run smoke` (offline+GitHub, no key) · `npm run pipeline` (full live pipeline + refine, needs a key).

## Done (verified green)
- Full 3-agent pipeline proven live; strict JSON schemas with 4 automated contract checks (variants ≥2, non-empty diagrams, citations resolve, table aligned)
- Model benchmarked and locked: **gemini-3.5-flash** (~26–40s full run; billing enabled, $10 budget alert)
- Real example runs generated by the pipeline itself (`examples.generated.ts`) — replaced all hand-written demo data
- Run history (localStorage, last 5, replayable)
- Production server + key proxy (`server.mjs`, stdlib-only) → repo is container-deployable anywhere, key never reaches browsers
- **Deployed live** on AI Studio → Cloud Run: blueprint-hackathon.ai.studio (verified in production incl. resilience: arXiv blocked by CORS in browser → run completed GitHub-only)
- **Refine loop** (`b138745`): instruction box under results → Architect re-drafts from the *same analyzed sources*; + mandatory "Rough monthly cost" row in the trade-off table. Refine ~16s, passes all checks headless.
- **Resilient papers rail** (`c0976d0`): 4 fallbacks — app proxy `/api/arxiv` → arXiv direct → public proxy → Semantic Scholar — so browser CORS can never leave a run paperless again
- Hygiene: no AI co-author trailers (history scrubbed), `.env` never committed (gate-checked), paste pack auto-generated (`npm run pastepack`)

## ⚠️ Live URL is one version behind the repo
The last publish predates the refine loop + papers rail. **Next action (owner, ~15 min): assembly** — re-paste 5 changed files into the AI Studio app (`types.ts`, `constants.ts`, `services/sources.ts`, `services/gemini.ts`, `App.tsx`), Preview a live topic (console should say "arXiv via app proxy: N papers found"), test the refine bar, Publish. Until then, don't judge the live URL by the latest features.

## Planned next (in order)
1. Assembly + republish (above)
2. **Component deep-dive** — click any diagram block → cited implementation brief (bridges idea→architecture toward architecture→code)
3. **Google Search grounding** as a third source rail (blogs/post-mortems next to papers+repos; Gemini-native)
4. **ADR export** — one click → Markdown Architecture Decision Record with citations
5. Sat: videos (scripts drafted), one-pager polish (draft exists — ask [OWNER]), full Sunday dry run per RUNBOOK.md
Rule for all of it: the app is always deployable; a feature isn't done until it's green *and* republished.

## How you can help right now (no key needed)
- **Try to break the live app**: weird topics, non-English, super vague, super niche. Screenshot anything ugly/wrong/slow into team chat — that's free QA.
- **Judge the outputs like a VC**: are variant names sharp? Diagrams sensible? Cost row believable? Which topics produce the *prettiest* results? (We need 1–2 hero topics for the demo video.)
- **Propose refine instructions that demo well** ("make it serverless", "cut cost 10x", "make it HIPAA-friendly"…) — the refine moment is our live-demo centerpiece.
- **Ideas for deep-dive & beyond**: what should clicking a component show? What would make YOU use this weekly?
- Read RUNBOOK.md + the pitch drafts; mark up anything that rings false.

## ⚠️ Cautions (please respect these)
1. **Builds/deploys run through [OWNER]'s AI Studio account and billed Google project.** Do not publish, redeploy, or instruct the AI Studio Build agent without coordinating — one deployer, one truth.
2. **The API key exists only in `.env` on [OWNER]'s Mac** (and server-side in the deployed app — same Google project). It is not in the repo (verified) and must never be pasted into chats, code, or commits.
3. **Every live pipeline run costs [OWNER] money** (cents per run — fine, but don't script loops against the live URL or run load tests).
4. Want to run locally? Clone → `npm i` → create your own key at aistudio.google.com/apikey → `.env` per `.env.example` → `npm run dev` or `npm run pipeline`. Your key, your (tiny) cost.
5. Repo hygiene: no co-author trailers in commits, `.env` stays untracked, prefer small commits with plain messages.
6. Sunday compliance: at the 10:30 briefing we confirm the pre-written-code policy; treat this repo as our rehearsal kit + snippet library and be ready to rebuild fast via `AISTUDIO_PROMPT.md` if required (it's designed for exactly that).